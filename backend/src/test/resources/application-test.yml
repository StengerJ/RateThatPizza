spring:
  datasource:
    url: jdbc:h2:mem:pgh_pizza;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret
  admin:
    email: admin@pgh-pizza.local
    password: ChangeMe123!
    display-name: PGH Admin
  mail:
    smtp-enabled: false
